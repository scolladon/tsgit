/**
 * @deprecated — Source of truth moved to `primitives/internal/ignore-predicate.ts`.
 * This shim keeps the existing command imports working; new callers should
 * import from the primitives location.
 */
export type { IgnorePredicate } from '../../primitives/internal/ignore-predicate.js';
